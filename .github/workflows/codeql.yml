name: CodeQL

on:
  push:
    branches: [main, develop, "release/**", "support/**"]
  pull_request:
    branches: [main, develop, "release/**", "support/**"]
  schedule:
    - cron: "23 4 * * 0"
  workflow_dispatch:

permissions: {}

jobs:
  analyze:
    name: CodeQL (go)
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: t33n-software/repository-governance/.github/workflows/reusable-codeql-go.yml@89be739ee8a1d1ed6ebbe97dd1556a253477d242
