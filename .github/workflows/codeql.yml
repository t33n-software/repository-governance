name: CodeQL

on:
  push:
    branches: [main, develop, "release/**", "support/**"]
  pull_request:
    branches: [main, develop, "release/**", "support/**"]
  schedule:
    - cron: "23 4 * * 0"
  workflow_dispatch:

permissions: {}

jobs:
  analyze:
    name: CodeQL
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: t33n-software/repository-governance/.github/workflows/reusable-codeql-go.yml@38c8ea236b858828b4d0134e04577117f96d4879
