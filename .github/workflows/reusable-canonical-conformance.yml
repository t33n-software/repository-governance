name: Canonical Conformance — reusable

on:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: canonical-conformance-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  conformance:
    name: Canonical bindings verification
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Verify canonical bindings
        uses: t33n-software/repository-governance/.github/actions/verify-canonical-files@73adc42b530261a06cfb9c25790a5c100ca97707
